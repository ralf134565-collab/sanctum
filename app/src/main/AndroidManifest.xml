<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Local-First приложение: НИ ОДНОГО android.permission.INTERNET.
        Это архитектурный инвариант — гарантия пользователю, что данные
        физически не могут утечь с устройства.
    -->

    <!-- Принудительно вырезаем разрешение на интернет, гарантируя приватность на уровне сборки -->
    <uses-permission android:name="android.permission.INTERNET" tools:node="remove" />

    <!-- WorkManager: служебный прогрев локальной модели ИИ после перезагрузки. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Тактильное ведение дыхательного моста (резонансный режим). -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".PocketReflectApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.PocketReflect"
        tools:targetApi="34">

        <!--
            Нативные библиотеки для GPU-бэкенда LiteRT-LM (Gemma 4).
            android:required="false" критично: на устройствах без OpenCL
            (драйвер отсутствует или старый Android) приложение всё равно
            устанавливается и работает — `LiteRtGemmaEngine` автоматически
            делает fallback на CPU-backend при `LiteRtLmJniException` /
            `UnsatisfiedLinkError`. Элемент действует с API 31 (Android 12);
            на minSdk=28..30 он молча игнорируется системой.
        -->
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false" />
        <uses-native-library
            android:name="libvndksupport.so"
            android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PocketReflect"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FileProvider для оффлайн-шаринга изображений -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Отключение автоматической инициализации WorkManager, так как мы инициализируем его вручную с HiltWorkerFactory -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
